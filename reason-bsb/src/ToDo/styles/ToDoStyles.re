let buttonStyle =
  ReactDOMRe.Style.make(
    ~cursor="pointer",
    ~minWidth="100px",
    ~borderRadius="4px",
    ~marginLeft="5px",
    (),
  );

let inputStyle =
  ReactDOMRe.Style.make(~padding="10px", ~borderRadius="4px", ());

let alertStyle = ReactDOMRe.Style.make(~color="red", ~fontWeight="bold", ());
